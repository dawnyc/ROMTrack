[ROMTrack]  # <tracker-name>
label = ROMTrack
protocol = traxpython

command = ROMTrack

# Specify a path to trax python wrapper if it is not visible (separate by ; if using multiple paths)
paths = /path_to_romtrack/lib/test/vot20
